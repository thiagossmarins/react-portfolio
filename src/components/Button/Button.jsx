import React from 'react';

const Button = () => {
    return (
        <div>
            <button className="btn">
                <a href="#contact">Let's Talk</a>
            </button>
        </div>
    );
};

export default Button;